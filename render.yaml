services:
  - type: web
    name: ehabgm-payment-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.6 # أو الإصدار اللي انت شغال عليه